//#define _CRT_SECURE_NO_WARNINGS
//// c++ STL库函数sort对结构体数组排序
//// c++ STL库函数sort对结构体数组排序
//#include<bits/stdc++.h>
//using namespace std;
//const int mxn = 5e3 + 5;
//int n;
//struct ST {
//    int h, m, s;
//    bool operator<(const ST& x) const {
//        if (h == x.h && m == x.m) return s < x.s;
//        else if (h == x.h) return m < x.m;
//        else return h < x.h;
//    }
//}a[mxn];
//signed main() {
//    scanf("%d", &n);
//    for (int i = 1; i <= n; ++i)
//        scanf("%d%d%d", &a[i].h, &a[i].m, &a[i].s);
//    sort(a + 1, a + n + 1);
//    for (int i = 1; i <= n; ++i)
//        printf("%d %d %d\n", a[i].h, a[i].m, a[i].s);
//    return 0;
//}

//大概懂了
//c++的东西，不懂