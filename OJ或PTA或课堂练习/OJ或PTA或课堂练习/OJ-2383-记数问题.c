//#define _CRT_SECURE_NO_WARNINGS
//#include<stdio.h>
//int n, x, i, ans, a[1000002];
//int main()
//{
//	scanf("%d%d", &n,&x);
//	for (int j = 1; j <= n; j++)a[j] = a[j / 10] + (j % 10 == x);
//	for (int j = 1; j <= n; j++)a[j] += a[j - 1];
//	printf("%d\n", a[n]);
//	return 0;
//}

//递推和前缀和，还不熟练