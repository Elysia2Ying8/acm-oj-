////#define _CRT_SECURE_NO_WARNINGS
////#include<stdio.h>
////int main()
////{
////	int a, b, f[10000000];
////	for (int i = 1; i <= 1000000; i++) {
////		f[i] = i % 10 + f[i / 10];
////	}
////	scanf("%d%d", &a, &b);
////	for (int i = a; i <= b; i++) {
////		f[0] += f[i] % 5 == 0;
////	}
////	printf("%d\n", f[0]);
////	return 0;
////}

//int f[1000000], a, b;
//signed main(void) {
//    for (int i = 1; i < 1000000; ++i) f[i] = i % 10 + f[i / 10];
//    scanf("%d%d", &a, &b);
//    for (int i = a; i <= b; ++i) f[0] += f[i] % 5 == 0;
//    printf("%d\n", f[0]);
//    return 0;
//}

//我的又不行
