//#define  _CRT_SECURE_NO_WARNINGS
//#include<bits/stdc++.h>
//using namespace std;
//const int mxn = 1e6 + 5;
//int n;
//int dp[mxn] = { 0,1,0,1,1 };
//signed main()
//{
//	ios::sync_with_stdio(0), cin.tie(0), cout.tie(0);
//	for (int i = 5; i < mxn; ++i)
//		dp[i] = !(dp[i - 4] && dp[i - 3] && dp[i - 1]);
//	cin >> n;
//	cout << (dp[n] ? "first" : "second") << '\n';
//	return 0;
//}