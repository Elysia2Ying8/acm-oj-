//#define _CRT_SECURE_NO_WARNINGS
//#include<stdio.h>
//int main()
//{
//	int N, i, sum = 0, a[100000];
//	scanf("%d", &N);
//	for (i = 0; i < N; i++) {
//		scanf("%d", &a[i]);
//	}
//	for (i = 0; i < N; i++) {
//		sum += a[i];
//		if (sum < 0)sum -= 2 * a[i];
//		else continue;
//	}
//	printf("%d", sum);
//	return 0;
//}