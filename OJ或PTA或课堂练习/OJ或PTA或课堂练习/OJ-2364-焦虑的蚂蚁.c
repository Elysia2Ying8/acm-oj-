//#define _CRT_SECURE_NO_WARNINGS
//#include<stdio.h>
//int main()
//{
//	int  n, m, l=0, r=0;
//	scanf("%d%d", &n, &m);
//	for (int i = 1; i <= m; i++) {
//		int d, p;
//		scanf("%d%d", &d, &p);
//		if (d == 0 && p > l)l = p;
//		else if (d == 1 && r < n - p)r = n - p;
//	}
//	printf("%d\n", l > r ? l : r);
//	return 0;
//}
//

//まだ尖盾できない