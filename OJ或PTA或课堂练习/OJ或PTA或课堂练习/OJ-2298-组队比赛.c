//#define _CRT_SECURE_NO_WARNINGS
//#include<bits/stdc++.h>
//using namespace std;
//int a[4], sum, ans = INT32_MAX;
//signed main() {
//    for (int i = 0; i < 4; ++i) scanf("%d", a + i), sum += a[i];
//    for (int i = 1; i < 4; ++i)
//        if (ans > abs(sum - 2 * a[0] - 2 * a[i])) ans = abs(sum - 2 * a[0] - 2 * a[i]);
//    printf("%d\n", ans);
//    return 0;
//}

//²»»á£¬³­µÄ