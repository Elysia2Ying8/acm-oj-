//#define _CRT_SECURE_NO_WARNINGS
//#include<stdio.h>
//#define int long long
//const int mxn = 1e4 + 5;
//int n, p[100000];
//signed main(void) {
//    for (int k, j = 1, i = 1; j < mxn; ++i) {
//        k = i;
//        while (j < mxn && k--) p[j++] = i;
//    }
//    for (int i = 1; i < mxn; ++i) p[i] += p[i - 1]; // 前缀和
//    scanf("%lld", &n);
//    printf("%lld\n", p[n]);
//    return 0;
//}

//不会，抄的

//讲过之后有点懂了，算法太弔辣