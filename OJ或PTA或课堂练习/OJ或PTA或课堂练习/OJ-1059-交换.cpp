//#define _CRT_SECURE_NO_WARNINGS
//#include<bits/stdc++.h>
//using namespace std;
//const int mxn = 1e6 + 5;
//int a[mxn], n, m;
//signed main()
//{
//	ios::sync_with_stdio(0), cin.tie(0), cout.tie(0);
//	cin >> n;
//	for (int i = 1; i <= n; ++i)cin >> a[i];
//	for (int i = 1; (2*i) <= n; ++i) {
//		int temp = a[i * 2];
//		a[2 * i] = a[i * 2 - 1];
//		a[2 * i - 1] = temp;
//	}
//	for (int i = 1; i <= n; ++i)
//		cout << a[i] << (i == n ? '\n' : ' ');
//	return 0;
//}