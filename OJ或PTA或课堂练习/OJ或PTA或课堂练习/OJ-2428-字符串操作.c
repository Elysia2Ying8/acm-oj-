//#define _CRT_SECURE_NO_WARNINGS
//#include<stdio.h>
//int n, m, l, r;
//char c1, c2, arr[15];
//int main()
//{
//    scanf("%d%d", &n, &m);
//    for (int i = 1; i <= n; i++) {
//        scanf("%c", &arr[i]);
//    }
//    for (int i = 1; i <= m; i++) {
//        scanf("%d%d%c%c", &l, &r, &c1, &c2);
//        for (int j = l; j <= r; j++) {
//            if (arr[j] == c1)arr[j] = c2;
//        }
//    }
//    for (int i = 1; i <= n; i++) {
//        printf("%c", arr[i]);
//    }
//    return 0;
//}

//不知道哪里出问题
//不会

//#define _CRT_SECURE_NO_WARNINGS
//#include<stdio.h>
//int n, m, l, r;
//char s[1000], c1[2], c2[2];
//signed main() {
//    scanf("%d%d%s", &n, &m, s + 1);
//    for (int i = 1; i <= m; ++i) {
//        scanf("%d%d%s%s", &l, &r, c1, c2);// 以读串的方式读字符
//        for (int j = l; j <= r; ++j)
//            if (s[j] == c1[0]) s[j] = c2[0];
//    }
//    printf("%s\n", s + 1);
//    return 0;
//}