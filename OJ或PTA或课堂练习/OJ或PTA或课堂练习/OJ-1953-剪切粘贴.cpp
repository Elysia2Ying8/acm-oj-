//#define  _CRT_SECURE_NO_WARNINGS
//#include<bits/stdc++.h>
//using namespace std;
//string s, t;
//int n;
//int l, r;
//string ll, rr;
//signed main()
//{
//	ios::sync_with_stdio(0), cin.tie(0), cout.tie(0);
//	cin >> s >> n;
//	while (n--) {
//		cin >> l >> r >> ll >> rr;
//		t = s.substr(l-1, r - l + 1);
//		s.erase(l-1, r - l + 1);
//		int p = s.find(ll + rr);
//		if (p == -1)s += t;
//		else s.insert(p + ll.size(), t);
//	}
//	cout << s << '\n';
//	return 0;
//}