//#define _CRT_SECURE_NO_WARNINGS
//#include<stdio.h>
//int main()
//{
//	int n, i, sum = 0, a[1000000];
//	scanf("%d", &n);
//	for (i = 0; i < n; i++) {
//		scanf("%d", &a[i]);
//	}
//	for (i = 0; i < n; i++) {
//		sum += a[i];
//		if (sum > 0)sum -= 2 * a[i];
//		else continue;
//	}
//	printf("%d", sum);
//	return 0;
//}