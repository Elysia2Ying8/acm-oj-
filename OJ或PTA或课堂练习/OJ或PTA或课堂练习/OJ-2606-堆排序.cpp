//#define _CRT_SECURE_NO_WARNINGS
//#include<bits/stdc++.h>
//using namespace std;
//const int mxn = 1e5 + 5;
//int n, m;
//int a[mxn];
//signed main()
//{
//	ios::sync_with_stdio(0), cin.tie(0), cout.tie(0);
//	cin >> n >> m;
//	for (int i = 0; i < n; ++i)cin >> a[i];
//	sort(a, a + n);
//	for (int i = 0; i < m; ++i)
//		cout << a[i] << (i == m - 1 ? '\n' : ' ');
//	return 0;
//}