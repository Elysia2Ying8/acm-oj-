//#define _CRT_SECURE_NO_WARNINGS
//#include<stdio.h>
////int main()
////{
////	long long a, b, n, m;
////	scanf("%lld%lld", &a, &b);
////	m = a / b;
////	if (m < 0)m -= 1;
////	n = a % b;
////	if (n < 0)n = -n;
////	printf("%lld %lld", a / b, n);
////	return 0;
////}
// 
// 
//#define int long long
//int a, b, ans, r;
//signed main() {
//    scanf("%lld%lld", &a, &b);
//    r = a - a / b * b;
//    if (r < 0) r += b;
//    ans = (a - r) / b;
//    printf("%lld %lld\n", ans, r);
//    return 0;
//}

//凭什么我的不行，妈的跟你爆了