//#define _CRT_SECURE_NO_WARNINGS
//#include<stdio.h>
//#define int long long
//int a[3], ans;
//signed main() {
//    for (int i = 0; i < 3; ++i) scanf("%lld", a + i);
//    for (int i = 0; i < 3; ++i)
//        ans += (int)sqrt(a[i] * a[(i + 2) % 3] / a[(i + 1) % 3] + 0.5);
//    printf("%lld\n", ans * 4);
//    return 0;
//}

//不懂，完全不懂