//#define  _CRT_SECURE_NO_WARNINGS
//#include<bits/stdc++.h>
//using namespace std;
//#define int long long 
//const int bit = 16;
//int n;
//int ans, cnt[bit];
//signed main()
//{
//	ios::sync_with_stdio(0), cin.tie(0), cout.tie(0);
//    int a;
//    cin >> n;
//    for (int i = 1; i <= n; ++i) {
//        cin >> a;
//        for (int j = 0; j < bit; ++j)cnt[j] += (a & (1 << j)) > 0;
//    }
//    for (int i = 0; i < bit; ++i)
//        ans += (cnt[i] * (cnt[i] - 1) + 2 * cnt[i] * (n - cnt[i])) * (1LL << i);
//    cout << ans << '\n';
//	return 0;
//}
