//#define  _CRT_SECURE_NO_WARNINGS
//#include<bits/stdc++.h>
//using namespace std;
//const int mxn = 2e3 + 5;
//int n;
//int a[mxn];
//int mx, mn;
//int ans;
//signed main()
//{
//	cin >> n;
//	for (int i = 1; i <= n; ++i) {
//		cin >> a[i];
//		if (a[i] == 1)mn = i;
//		else if (a[i] == n)mx = i;
//	}
//	ans = max(ans, mn - 1);
//	ans = max(ans, mx - 1);
//	ans = max(ans, n - mn);
//	ans = max(ans, n - mx);
//	cout << ans << '\n';
//	return 0;
//}