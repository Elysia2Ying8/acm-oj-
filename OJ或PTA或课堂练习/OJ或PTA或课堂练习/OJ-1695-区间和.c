//#define _CRT_SECURE_NO_WARNINGS
//#include<stdio.h>
//int main()
//{
//	long long a[1005] = { 0 }, n, m, l, r, sum[1005] = { 0 };
//	scanf("%lld%lld", &n, &m);
//	for (int i = 0; i < n; i++) {
//		scanf("%lld", &a[i]);
//	}
//	for (int i = 0; i < m; i++) {
//		scanf("%lld%lld", &l, &r);
//		for (int j = 0; j < n; j++) {
//			if (a[j] >= l && a[j] <= r)sum[i] += a[j];
//		}
//	}
//	for (int i = 0; i < m; i++) {
//		printf("%lld\n", sum[i]);
//	}
//	return 0;
//}
//
//鯵の嬬薦はまだできません、瘁で