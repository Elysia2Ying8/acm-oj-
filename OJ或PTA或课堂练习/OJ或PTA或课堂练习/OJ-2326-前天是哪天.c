//#define _CRT_SECURE_NO_WARNINGS
//#include<stdio.h>
//int y, m, d;
//int mm[] = { 0, 31, 0, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31 };
//int check(int year);
//void pre();
//
//int main()
//{
//	scanf("%d-%d-%d", &y, &m, &d);
//	pre(), pre();
//	printf("%4d-%02d-%02d", y, m, d);
//	return 0;
//}
//
//int check(int year)
//{
//	if (year % 400 == 0)return 1;
//	else if (year % 100 == 0)return 0;
//	else if (year % 4 == 0)return 1;
//	else return 0;
//}
//
//void pre()
//{
//	mm[2] = check(y) ? 29 : 28;
//	if (m == 1 && d == 1)y -= 1, m = 12, d = 31;
//	else if (d == 1)m -= 1, d = mm[m];
//	else d -= 1;
//}

//重要！！！
//函数的使用
//%02的意思
//return的用法
//打表
//数组的运用
//运用两次函数使问题简化