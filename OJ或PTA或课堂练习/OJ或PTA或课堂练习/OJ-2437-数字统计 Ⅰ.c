//#define _CRT_SECURE_NO_WARNINGS
//#include<bits/stdc++.h>
//using namespace std;
//#define int long long
//const int mxn = 1e6 + 5;
//int l, r, a[mxn] = { 0,0,1 };
//signed main()
//{
//    scanf("%lld%lld", &l, &r);
//    for (int i = 1; i <= r; i++)a[i] = a[i / 10] + a[i % 10];
//    for (int i = 1 + 1; i <= r; i++)a[i] += a[i - 1];
//    printf("%lld\n", a[r] - a[--l]);
//    return 0;
//}